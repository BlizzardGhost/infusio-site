import { Sun } from "../astres/Sun";
import { Planet } from "../astres/Planet";
export declare const generateSolarSytem: ({ planets, sunScale, scale, rotationSpeed, distance, ctx, }: {
    planets: (Planet | Sun)[];
    sunScale: number;
    scale: number;
    rotationSpeed: number;
    distance: number;
    ctx: CanvasRenderingContext2D;
}) => (Sun | Planet)[];
