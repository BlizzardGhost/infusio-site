---
// src/components/HeroVoice.astro
const title    = 'Infuse your business with calm, clarity, and AI.';
const subtitle = 'We build fast sites and helpful assistants that reduce busywork and stress. Clear plans. Solid foundations. Guidance from start to launch.';
const prompt   = 'Tap the orb to speak. I’ll listen and can book a call.';
---
<section id="top" class="hero-full hero--centered" aria-label="Hero">
  <div class="hero-inner">
    <span class="badge">Astro + Vercel • Automations • Friendly support</span>
    <h1 class="hero-title">{title}</h1>
    <p class="hero-sub">{subtitle}</p>

    <!-- Single CTA: the voice orb -->
    <button id="voice-orb" class="voice-orb" aria-label="Talk to the AI Receptionist" data-backend-url={import.meta.env.PUBLIC_BACKEND_URL}>
      <span class="pulse" aria-hidden="true"></span>
      <span class="glow" aria-hidden="true"></span>
      <span class="iconify" data-icon="mingcute:voice-line" aria-hidden="true"></span>
    </button>

    <p id="voice-status" class="voice-status" role="status" aria-live="polite">{prompt}</p>
  </div>
</section>